defaults:
  - robotwin_omnigen2_ee16
  - _self_

# Same periodic clean split as the 14D RoboTwin clean configuration.
train:
  episode_index_filter:
    mode: periodic_prefix
    period: 550
    keep_first: 50

val:
  episode_index_filter: ${data.train.episode_index_filter}
